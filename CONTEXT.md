# UsingYourTime

휴대폰을 잠금 해제한 뒤 끊김 없이 계속 사용한 시간을 사용자에게 알려주는 Android 앱의 도메인 언어.

## Language

**연속 사용 세션 (Session)**:
잠금 해제로 시작해 잠금(또는 화면 꺼짐)으로 끝나는 한 번의 사용 구간. 잠금 화면이 '없음'인 기기에서는 화면 켜짐이 잠금 해제를 대신하므로 화면 켜짐~꺼짐이 한 세션이다. 유예 시간 안에 다시 잠금 해제되면 같은 세션이 이어진다.
_Avoid_: 사용 시간, 스크린 타임, 화면 켜짐 시간

**유예 시간 (Grace period)**:
잠금 후 이 시간 안에 다시 잠금 해제되면 세션이 끊기지 않고 이어지는 허용 간격.
_Avoid_: 쿨다운, 타임아웃

**연속 사용 시간 (Session duration)**:
현재 세션이 시작된 뒤 지금까지 흐른 시간. 유예 시간 동안 잠겨 있던 구간도 포함한다.
_Avoid_: 누적 사용 시간, 총 사용 시간

**임계값 (Threshold)**:
연속 사용 시간이 이 값에 도달하면 임계값 알림이 발생하는 기준 시간.
_Avoid_: 제한 시간, 리밋

**상시 표시 (Persistent display)**:
측정이 켜진 동안 상태바에 계속 노출되는 알림. 세션 중에는 현재 연속 사용 시간을 보여주고, 세션이 없을 때는 측정 대기 중임을 보여준다. 사용자가 지워도 측정이 켜져 있으면 다시 나타난다.
_Avoid_: 포그라운드 알림, 위젯

**임계값 알림 (Threshold alert)**:
연속 사용 시간이 임계값에 도달했을 때 사용자의 주의를 끌기 위해 보내는 알림. 잠금 중(유예 시간)에는 발생하지 않고, 유예 안에 다시 잠금 해제된 직후 발생한다.
_Avoid_: 경고, 푸시

**재알림 (Re-alert)**:
임계값 알림 뒤에도 세션이 이어질 때, 재알림 주기마다 다시 보내는 임계값 알림. 사용자가 알림을 지웠는지와 무관하게 반복되며, 세션 알림 끄기를 한 세션에서만 멈춘다.
_Avoid_: 리마인더, 반복 알림

**재알림 주기 (Re-alert interval)**:
직전 임계값 알림(또는 재알림)으로부터 다음 재알림까지의 간격. 잠금 중에 주기가 지나면 다음 잠금 해제 직후 한 번만 재알림하고, 주기는 그 알림부터 다시 센다.
_Avoid_: 스누즈 시간, 알림 간격

**추정 종료 시각 (Estimated end time)**:
측정이 비정상적으로 끊겨 잠금을 관측하지 못했을 때 세션의 종료로 삼는 시각. 마지막으로 측정이 살아 있던 시각과 마지막 잠금 시각 중 늦은 쪽이다. 측정이 다시 살아난 시점까지의 공백이 유예 시간 이내면 세션이 이어지고, 넘으면 이 시각에 세션이 끝난 것으로 본다. 재부팅도 예외가 아니다.
_Avoid_: 크래시 시각, 마지막 heartbeat

**측정 시작 (Start tracking)**:
사용자가 세션 측정을 켜는 명시적 행위. 한 번 켜면 측정 중지를 누르기 전까지 기기 재부팅·앱 업데이트·강제 종료 뒤에도 켜진 상태가 이어지며, 시스템이 되살리지 못한 경우에는 다음 앱 실행 때 자동으로 재개된다.
_Avoid_: 서비스 시작, 추적 시작, 활성화

**측정 중지 (Pause)**:
사용자가 세션 측정과 모든 알림을 명시적으로 멈추는 행위. 설정 화면에서만 할 수 있다. 진행 중이던 세션은 중지 시점에 유예 시간 없이 종료되고, 다시 측정 시작을 누르기 전까지 자동으로 재개되지 않는다.
_Avoid_: 스누즈, 무시, 끄기

**세션 알림 끄기 (Mute session)**:
현재 세션이 끝날 때까지만 임계값 알림과 재알림을 멈추는 행위. 임계값 알림의 액션 버튼으로 하며, 상시 표시와 측정은 계속된다. 세션이 확정 종료되면 자동으로 풀리고, 세션 중에 되돌릴 수 없다.
_Avoid_: 스누즈, 뮤트, 이번만 무시
